# 🔄 Auto-Refresh Setup

## สิ่งที่ตั้งค่าไว้แล้ว

### 1. Nodemon Auto-Restart ✅
เมื่อคุณแก้ไขไฟล์ใดก็ตาม nodemon จะรีสตาร์ทเซิร์ฟเวอร์อัตโนมัติ

**ไฟล์ที่ watch:**
- `src/**/*` - โค้ดทั้งหมดใน src
- `prisma/schema.prisma` - Prisma schema
- `.env`, `.env.local` - Environment variables

**วิธีใช้:**
```bash
npm run dev:watch
```

### 2. Next.js Turbopack Hot Reload ⚡
Turbopack จะทำ Hot Module Replacement (HMR) อัตโนมัติเมื่อคุณแก้ไขไฟล์

**ไม่ต้องทำอะไร** - มันจะ refresh หน้าเว็บทันทีเมื่อโค้ดเปลี่ยน!

## การใช้งาน

### เริ่มเซิร์ฟเวอร์
```bash
npm run dev:watch
```

### เมื่อแก้ไขโค้ด
1. **React Components** → Turbopack จะ Hot Reload ทันที (ไม่ต้อง refresh)
2. **API Routes** → nodemon จะรีสตาร์ทเซิร์ฟเวอร์ (ต้อง refresh)
3. **Prisma Schema** → nodemon จะรีสตาร์ท (ควร run `npm run db:generate`)

### Hard Refresh (ล้างแคช)
เมื่อต้องการล้างแคชบราวเซอร์:

**Windows:**
- `Ctrl + Shift + R`
- หรือ `Ctrl + F5`

**Mac:**
- `Cmd + Shift + R`
- หรือ `Cmd + F5`

## ตัวอย่างการทำงาน

```
[แก้ไข src/components/settings/team-settings.tsx]
   ↓
[Turbopack Hot Reload] ⚡
   ↓
[หน้าเว็บอัพเดททันที - ไม่ต้องกด refresh!]
```

```
[แก้ไข src/app/api/team/members/route.ts]
   ↓
[nodemon รีสตาร์ทเซิร์ฟเวอร์] 🔄
   ↓
[กด Ctrl+Shift+R เพื่อ hard refresh]
```

## Tips

### เร็วที่สุด 🚀
- แก้ไข React Components → Auto refresh
- ไม่ต้องรอรีสตาร์ทเซิร์ฟเวอร์

### ควร Hard Refresh 🔄
- เมื่อแก้ไข API routes
- เมื่อเปลี่ยน environment variables
- เมื่อโค้ดไม่อัพเดทตามที่คาดหวัง

### ล้างแคชเต็มรูปแบบ 🧹
```bash
# ลบ .next cache
Remove-Item -Recurse -Force ".next"

# เริ่มเซิร์ฟเวอร์ใหม่
npm run dev:watch
```

## ปัญหาที่อาจเจอ

### Port 4000 ถูกใช้งานอยู่
```powershell
# ปิด process ที่ใช้ port 4000
$process = Get-NetTCPConnection -LocalPort 4000 | Select-Object -ExpandProperty OwningProcess -First 1
Stop-Process -Id $process -Force
```

### nodemon ไม่ restart
```bash
# พิมพ์ rs ใน terminal แล้วกด Enter
rs
```

### หน้าเว็บไม่อัพเดท
1. กด `Ctrl + Shift + R` (Hard Refresh)
2. เช็ค Console ว่ามี error หรือไม่
3. ลอง clear .next cache

## File Configuration

### nodemon.json
```json
{
  "watch": ["src", "prisma/schema.prisma", ".env", ".env.local"],
  "ext": "ts,tsx,js,jsx,json",
  "ignore": ["node_modules", ".next"],
  "exec": "next dev --turbopack -p 4000",
  "delay": 1000
}
```

### package.json
```json
{
  "scripts": {
    "dev:watch": "nodemon"
  }
}
```

## สรุป

✅ **nodemon** - Auto-restart เซิร์ฟเวอร์เมื่อแก้ไขไฟล์  
✅ **Turbopack HMR** - Hot reload React components ทันที  
✅ **Ctrl+Shift+R** - Hard refresh เมื่อต้องการล้างแคช  

**ไม่ต้องทำอะไรเพิ่มเติม - มันทำงานอัตโนมัติ!** 🎉
